Call the `get_ticket_suggestions` tool to get AI-scored recommendations on what to work on next. Scores based on priority, due dates, in-progress status, bug type, and blocker status.

If the user provided arguments: $ARGUMENTS — pass as the `project` parameter if it looks like a project key.
