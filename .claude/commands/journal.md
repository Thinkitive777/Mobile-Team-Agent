Call the `journal` tool to add a real-time work log entry. Journal entries are timestamped and auto-linked to any Jira ticket keys mentioned in the text.

Journal entry: $ARGUMENTS

If no arguments provided, call `show_journal` instead to display today's journal.
