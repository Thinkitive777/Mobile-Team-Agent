Call the `get_recent_commits` tool to show recent git commits with file-level diff stats (files changed, lines added/removed) and work area analysis (which parts of the codebase were touched).

If the user provided arguments: $ARGUMENTS — pass as the `since` parameter (e.g. "7 days ago", "yesterday", "24 hours ago").
