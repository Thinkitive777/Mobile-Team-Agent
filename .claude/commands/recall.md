Call the `recall` tool to search persistent memory for past notes, decisions, and journal entries.

Search query: $ARGUMENTS

- If the argument looks like a ticket key (e.g. PROJ-123), use `recall` with `ticket_key` parameter
- Otherwise use `recall` with `query` parameter
- If no arguments, call `recall` with no parameters to show today's memory context
