Call the `select_ticket` tool with the provided ticket key to show full details: description, comments, subtasks, linked issues, saved memory notes, and suggest an implementation plan.

The user must provide a ticket key: $ARGUMENTS

If no arguments provided, ask the user for a ticket key (e.g. "PROJ-123").
