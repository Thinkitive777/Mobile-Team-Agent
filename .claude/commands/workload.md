Call the `analyze_workload` tool to categorize all assigned tickets: Done, In Progress, Not Started, Blocked, Overdue. Includes smart blocker detection via issue links, labels, and status text.

If the user provided arguments: $ARGUMENTS — pass as the `project` parameter if it looks like a project key.
