Call the `plan_my_day` tool to generate a deep daily plan. This analyzes tickets (new, pending, blocked, overdue), reads comments for context, checks yesterday's completed work, surfaces saved memory/decisions, and produces a prioritized action plan.

If the user provided arguments: $ARGUMENTS — pass them as the `project` parameter if it looks like a project key.
