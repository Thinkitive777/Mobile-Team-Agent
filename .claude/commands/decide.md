Call the `add_decision` tool to record a decision or agreement that persists across sessions. Decisions stay active until explicitly resolved and surface in daily planning.

Decision text: $ARGUMENTS

Ticket keys mentioned in the text are auto-linked.

If no arguments provided, call `show_decisions` instead to display active decisions.
