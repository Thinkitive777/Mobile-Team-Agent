Call the `show_decisions` tool to display active (unresolved) decisions.

If the user provided arguments: $ARGUMENTS
- If "all" or "include resolved", pass `include_resolved=true`
- If it looks like a decision ID, call `resolve_decision` with that ID instead
