Call the `remember` tool to save a note to persistent memory. The note survives across sessions.

The user's note: $ARGUMENTS

If the text contains a Jira ticket key (e.g. PROJ-123), it will be auto-linked to that ticket.

If no arguments provided, ask the user what they want to remember.
