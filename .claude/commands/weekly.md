Call the `weekly_summary` tool to generate a weekly summary aggregating the last 7 daily reports: total commits, items completed, items still in progress, and blockers encountered.

If the user provided arguments: $ARGUMENTS — pass as the `end_date` parameter if it looks like a date (YYYY-MM-DD).
