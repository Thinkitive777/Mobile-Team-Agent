{
  "name": "project-guide-agent",
  "version": "2.1.0",
  "description": "Production-ready developer assistant with Jira + Git integration, daily automation, and intelligent workflow management",
  "main": "index.js",
  "bin": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "pkg . --out-path dist/",
    "build-zip": "zip -r projectguide-agent.zip dist/ install.sh install.bat agent_prompt.md index.js jira-client.js git-utils.js report-manager.js constants.js errors.js logger.js validators.js package.json README.md .env.example",
    "validate": "node -c index.js && node -c jira-client.js && node -c git-utils.js && node -c report-manager.js && node -c constants.js && node -c errors.js && node -c logger.js && node -c validators.js && echo 'All files valid'"
  },
  "pkg": {
    "assets": [],
    "targets": [
      "node18-macos-x64",
      "node18-win-x64",
      "node18-linux-x64"
    ]
  },
  "keywords": ["jira", "git", "developer-assistant", "automation", "mcp", "standup", "reporting"],
  "author": "ProjectGuide Team",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "dotenv": "^17.3.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "pkg": "^5.8.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
